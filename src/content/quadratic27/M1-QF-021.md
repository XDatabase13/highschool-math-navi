---
problem_id: M1-QF-021
version: 1
subject: 数学I
unit: 二次関数
section: 最大・最小
display_order: 21
title: 定義域全体が文字によって動く最大・最小
difficulty: 4
importance: 3
importance_label: 本命
reference_problem: "158"
verification_status: 独立検算済み
assets:
  - file: flow-(1)-02.svg
    placement: flow
    part: 1
    flow: 2
    type: graph-compare
    purpose: ""
    must_show: []
    must_not_show: []
  - file: flow-(1)-03.svg
    placement: flow
    part: 1
    flow: 3
    type: graph-compare
    purpose: ""
    must_show: []
    must_not_show: []
  - file: flow-(2)-02.svg
    placement: flow
    part: 2
    flow: 2
    type: graph-compare
    purpose: ""
    must_show: []
    must_not_show: []
  - file: flow-(2)-03.svg
    placement: flow
    part: 2
    flow: 3
    type: graph-compare
    purpose: ""
    must_show: []
    must_not_show: []
---

## 問題

$a$ を実数とする。関数

$$
y=x^2-4x+5 \qquad (a\leqq x\leqq a+2)
$$

について、次の問いに答えよ。

(1) 最小値を求めよ。  
(2) 最大値を求めよ。

## 問題メタ

本問題は範囲に文字が入っている最大・最小問題の中でも、範囲の両端に文字が含まれている問題で少し難易度は高め。しかし実際に解いている感覚では、式に文字が含まれているパターンに近く、もしそのパターンが習得できていれば、比較的スムーズにこの問題にも取り組めると思われる。①範囲に文字、②式に文字、の2パターンの問題がそれぞれ、テストの後ろの方で出題されるのは、良くあることである。

## 解法メタ

平方完成からグラフ化の流れでOK。両端に文字が含まれていてもやることは同じで、グラフ化も同じようにやることとなる。1点だけ特筆すべきなのは、「範囲の内部に頂点が含まれているパターン」。グラフ化はいつもと同じだが、そのグラフをaの範囲で対応させると $a\leqq 2\leqq a+2$ となり、一瞬どう解くのか迷うかもしれない。このまま解けるようになってもいいし、他の2パターンのaの範囲を確定させてから、余ったところをこの範囲とする手もある。

## ThinkingFlow

### (1) 最小値

#### 1. 平方完成

$y=x^2-4x+5$ を平方完成し、頂点の座標が分かる形にする。今回は定義域 $[a,a+2]$ の両端が、幅2を保ったまま丸ごと文字 $a$ で動く点が017・018と違うところ。

$$
y=(x-2)^2+1
$$

#### 2. ☆☆ グラフ化（3パターン）

下に凸のグラフなので、頂点が動く定義域の中に入っていれば、そこで最小値をとる。定義域が頂点より完全に左にある・頂点を含む・完全に右にある、の3通りをイメージする。動く定義域 $[a,a+2]$ と頂点 $x=2$ の位置関係を3パターンで考える。

#### 3. ☆ $a$ の範囲を各グラフに対応させる

定義域の右端 $a+2$ が頂点2に届いているか、左端 $a$ が頂点2を超えているかを比較する。頂点がちょうど定義域の内側に入るときは、$a$ 自体の範囲として表す必要がある。

$$
a<0,\qquad 0\leqq a\leqq2,\qquad 2<a
$$

#### 4. それぞれの最小値を確認

③で分けた3つの場合それぞれに、②で確認した「頂点が中にあれば頂点、外にあれば近い端点」という関係を当てはめる。

- $a<0$ のとき、$x=a+2$ で最小。
- $0\leqq a\leqq2$ のとき、$x=2$ で最小。
- $a>2$ のとき、$x=a$ で最小。

### (2) 最大値

#### 1. 平方完成

式は(1)と共通なので、頂点も同じく $(2,1)$ のままである。今度は最大値を考えるので、定義域の両端のうち頂点から遠い方に注目する。

$$
y=(x-2)^2+1
$$

#### 2. ☆☆ グラフ化（3パターン）

下に凸のグラフだが、最大値を考える場合はいつも通り定義域の両端のうち頂点から遠い方が候補になる。定義域が動くことで、その「遠い方」が入れ替わる場面を考える。定義域の両端のうち、頂点から遠い側がどちらになるかを見る。

#### 3. $a$ の範囲を各グラフに対応させる

左端 $a$ から頂点2までの距離と、右端 $a+2$ から頂点2までの距離を比べる。この2つの距離が等しくなるところが境目になる。

$$
a<1,\qquad a=1,\qquad 1<a
$$

#### 4. それぞれの最大値を確認

③で見つけた境目をもとに、頂点からより遠い端点の方が最大値を与える、という②の関係を当てはめる。

- $a<1$ のとき、$x=a$ で最大。
- $a=1$ のとき、$x=1,3$ で最大。
- $a>1$ のとき、$x=a+2$ で最大。

## 最終解答

### (1)

$$
\begin{cases}
a^2+1 & (a<0)\quad [x=a+2]\\
1 & (0\leqq a\leqq2)\quad [x=2]\\
a^2-4a+5 & (2<a)\quad [x=a]
\end{cases}
$$

### (2)

$$
\begin{cases}
a^2-4a+5 & (a<1)\quad [x=a]\\
2 & (a=1)\quad [x=1,3]\\
a^2+1 & (1<a)\quad [x=a+2]
\end{cases}
$$

## 備考

実解後、難易度3への再評価を検討。場合分けは3パターンだが、基本手順は既習の文字入り最大・最小と同型。
